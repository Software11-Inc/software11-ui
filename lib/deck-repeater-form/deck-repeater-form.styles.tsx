import { SxProps } from "@mui/joy/styles/types";

export const repeaterStyles: SxProps = {
  display: "flex",
  flexDirection: "column",
};

export const columnStyle: SxProps = {
  display: "flex",
  flexDirection: "column",
  gap: 1,
  p: 1,
};

export const rowStyle: SxProps = {
  display: "flex",
  flexDirection: "row",
  alignItems: "center",
};
